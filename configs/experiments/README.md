# Experiment configs (Directive #9)

| Config | Status |
|---|---|
| `options_historical_risk_study_v1.yaml` | Pre-registered; freeze after formation+validation |

Holdout (2025) requires `status: frozen-for-holdout` **and** Issue #3 `FINAL CONFIGURATION FROZEN`.
