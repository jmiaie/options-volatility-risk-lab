# Options historical risk validation — FROZEN FOR HOLDOUT
# Status: frozen-for-holdout
# FINAL CONFIGURATION FROZEN after formation (2015-2023) + validation (2024).
# Holdout (2025) may now be evaluated once; do not retune after this freeze.

experiment_id: options_hist_risk_v1
status: frozen-for-holdout
directive: 9
dataset_id: yf_options_risk_underlyings_daily_2015_2025_v1
primary_symbol: SPY
seed: 0

universe:
  symbols: [SPY, QQQ, IWM]
  interval: 1d
  description: >
    Liquid US index ETFs commonly used as options underlyings.
    Underlyings only via free yfinance — no paid options tapes.

periods:
  requested_data_start: "2015-01-01"
  requested_data_end_exclusive: "2026-01-01"
  formation_dev: {start: "2015-01-01", end_inclusive: "2023-12-31"}
  validation: {start: "2024-01-01", end_inclusive: "2024-12-31"}
  holdout: {start: "2025-01-01", end_inclusive: "2025-12-31"}
  holdout_access: allowed_after_final_configuration_frozen

risk:
  # Existing lab: compute_var_es, historical_simulation_var, delta_normal_var,
  # kupiec_pof_test, christoffersen_independence_test, conditional_coverage_test
  confidence_level: 0.95
  var_lookback: 252
  equity_notional: 1.0
  realized_vol_window: 21

stylized_book:
  # Stylized short ATM European call + optional equity; marked with formation
  # realized vol as BSM sigma (NOT market IV). No options tape.
  equity_shares: 0.0
  option_contracts: -1.0
  option_T: 0.0821917808219178  # 30/365
  rate: 0.02
  dividend_yield: 0.01
  multiplier: 100.0

hedging:
  # Existing simulate_delta_hedge; GBM paths calibrated to empirical vols.
  option_T: 1.0
  n_steps: 52
  cost_rate: 0.0005
  n_seeds: 50
  option_qty: -1.0

evaluation:
  invent_metrics: false
  report_nulls_exactly: true
  components:
    - realized_volatility
    - equity_rolling_hs_var_kupiec_christoffersen
    - stylized_option_book_hs_and_delta_normal_var
    - discrete_hedging_error_under_empirical_vols

constraints:
  no_2025_evaluation_until_freeze: false
  no_retune_after_freeze: true
  no_universe_change_after_dataset_freeze: true
  no_ci_network_fetch: true
  no_paid_options_tapes: true
  no_invented_option_panels: true

freeze_record:
  frozen_for_holdout_utc: "2026-09-16T04:00:00Z"
  basis: >
    Formation (2015-2023) + validation (2024) completed under pre-registered defaults.
    No hyperparameter search. confidence_level=0.95, var_lookback=252,
    stylized short ATM call (formation realized vol as BSM mark), weekly
    discrete hedge (n_steps=52, cost_rate=0.0005, n_seeds=50) unchanged.
    No 2025 access before this freeze.

notes: >
  FINAL CONFIGURATION FROZEN. Parameters locked to pre-registered defaults.
  Honest scope: underlyings + realized vol + VaR/ES backtests + stylized BSM
  option book + GBM hedging error. No market options surfaces.
