"""Reserved for a future lightweight visualization/reporting layer.

Intentionally empty: per the project brief, no dashboard or viz layer is
built until the numerical core and its tests are green, and it would never
substitute for the validation in ``tests/`` and ``docs/model-risk.md``.
"""
